/**
*main - this program prints all digits of base 10
*Return: 0 upon success
*/

#include <stdio.h>

int main(void)
{
	int num = 0;

	while (num < 10)
	{
		printf("%d\n", num);
		num++;
	}
	return (0);
}
