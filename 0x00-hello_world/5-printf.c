#include <stdio.h>

/**
*main - This program prints to the console or stdout
*description - uses some set boundary to print
*Return: Returns 0 on success
*/

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
